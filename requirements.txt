# Minimal requirements for FastAPI backend with Gemini, Pinecone, and embeddings

# FastAPI and server
fastapi==0.110.2
uvicorn==0.29.0

# Gemini
google-generativeai==0.3.2

# Pinecone (v2.x stable)
pinecone-client==2.2.4

# Embeddings (CPU only, no torch)
sentence-transformers==2.2.2
transformers==4.30.2
huggingface-hub==0.16.4
scikit-learn==1.7.0
scipy==1.16.0
numpy>=1.22.0
nltk==3.9.1
sentencepiece==0.2.0

# Utilities
python-dotenv==1.0.0
requests==2.32.4
pyyaml>=5.1
loguru>=0.5.0
tqdm==4.67.1

# Remove torch, torchvision, pillow, playwright, beautifulsoup4, etc. for minimal image
